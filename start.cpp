/* This file will add 2000 to the balance of the player if he crossed start.
	ALGORITHM:
	1.) Take the player's pointer as an argument
	2.) Take the 'rounds' vector which has the round number of the player after every throw
	3.) Check the last two elements. If they differ by one, add 2000 to his balance
*/
#include "class_defintions.h"
void start_class::transaction(player *current_player)
{

}
